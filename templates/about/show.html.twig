{% extends 'backoffice.html.twig' %}

{% block title %}About{% endblock %}

{% block content %}
<div class="container pt-5 pb-5">
    <h1>About</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ about.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ about.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ about.description }}</td>
            </tr>
        </tbody>
    </table>

    <button type="button " class="btn btn-outline-success"> <a href="{{ path('about_index') }}">back to list</a></button>

    <button type="button " class="btn btn-outline-success"><a href="{{ path('about_edit', {'id': about.id}) }}">edit</a></button>

    {{ include('about/_delete_form.html.twig') }}

    </div>
{% endblock %}
